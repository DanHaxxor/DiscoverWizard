# Copy to .env and fill in. .env is gitignored; never commit real tokens.
# Non-secret values are also baked into catalyst-config.json env_variables
# so deploys don't need .env — this file is for local `catalyst serve`.

LLM_API_URL=https://api.catalyst.zoho.com/quickml/v2/project/24095000000137016/llm/chat
LLM_MODEL=crm-di-qwen_text_14b-fp8-it
CATALYST_ORG_ID=854436641

# Secrets — Self Client credentials from api-console.zoho.com.
# Function mints short-lived access tokens from these on each call (cached).
CLIENT_ID=
CLIENT_SECRET=
REFRESH_TOKEN=
